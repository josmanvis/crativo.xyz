---
phase: 01-landing-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "npm run dev starts the app without errors on localhost:3000"
    - "Browser shows a styled page (not unstyled HTML or error screen)"
    - "npm run build produces a successful production build"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: "src/app/layout.tsx"
      provides: "Root layout with global styles"
      exports: ["default"]
    - path: "src/app/page.tsx"
      provides: "Landing page entry point"
      exports: ["default"]
    - path: "tailwind.config.ts"
      provides: "Tailwind CSS configuration"
      contains: "content"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
    - from: "next.config.ts"
      to: "package.json"
      via: "Next.js configuration"
      pattern: "nextConfig"
---

<objective>
Scaffold the Next.js 15 project with Tailwind CSS v4, TypeScript, and Remotion as dependencies. Establish the foundational file structure, dev scripts, and a minimal landing page placeholder.

Purpose: Every subsequent plan depends on this scaffold existing and running correctly.
Output: A working Next.js app that starts, builds, and renders a styled page.
</objective>

<execution_context>
@/Users/jose/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jose/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with dependencies</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    tailwind.config.ts
    postcss.config.mjs
    src/app/globals.css
    .gitignore
  </files>
  <action>
    Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --yes` in the project root (it will scaffold into the existing directory).

    After scaffolding, install Remotion dependencies:
    `npm install remotion @remotion/cli @remotion/player react react-dom`

    Verify package.json has: next (v15+), react (v19+), remotion, @remotion/cli, @remotion/player, tailwindcss, typescript.

    Update next.config.ts to add webpack config for Remotion compatibility:
    - Add `webpack: (config) => { config.externals = [...(config.externals || []), { canvas: "canvas" }]; return config; }` to suppress canvas warnings in SSR.

    Ensure .gitignore includes: node_modules, .next, out, .env*.local
  </action>
  <verify>
    Run `npm run dev` and confirm process starts without errors.
    Run `npm run build` and confirm successful build output.
    Check that `node_modules/remotion` and `node_modules/@remotion/player` exist.
  </verify>
  <done>
    Project starts on localhost:3000, builds cleanly, all dependencies installed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create minimal landing page with dark theme</name>
  <files>
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
  </files>
  <action>
    Update src/app/globals.css to set a dark theme base:
    - Body background: near-black (#0a0a0a or similar)
    - Text color: white/off-white
    - Remove any default Next.js boilerplate styles (keep Tailwind directives)

    Update src/app/layout.tsx:
    - Set metadata: title "crativo.xyz", description "Portfolio showcasing a decade of creative software projects"
    - Add Inter font from next/font/google
    - Ensure dark background applied via body className

    Update src/app/page.tsx to render a full-screen centered placeholder:
    - Full viewport height (min-h-screen)
    - Centered text: "crativo" in large font (text-6xl or text-8xl, font-bold)
    - Subtitle below: "Loading experience..." in muted gray
    - This placeholder will be replaced by the Remotion player in Plan 02
    - Use flexbox centering (flex items-center justify-center)
  </action>
  <verify>
    Run `npm run dev`, open localhost:3000 in browser.
    Page shows dark background with centered "crativo" text and subtitle.
    No console errors. No layout shift or flash of white.
  </verify>
  <done>
    Dark-themed landing page renders with centered branding text, ready to be replaced by Remotion animation in next plan.
  </done>
</task>

</tasks>

<verification>
- `npm run dev` starts without errors
- `npm run build` completes without errors
- localhost:3000 shows dark-themed page with "crativo" centered
- No TypeScript errors (`npx tsc --noEmit`)
- Remotion packages are in node_modules
</verification>

<success_criteria>
- Working Next.js 15 + Tailwind + Remotion project
- Dev server runs, production build succeeds
- Dark-themed landing page renders correctly
- All dependencies properly installed
</success_criteria>

<output>
After completion, create `.planning/phases/01-landing-experience/01-01-SUMMARY.md`
</output>
